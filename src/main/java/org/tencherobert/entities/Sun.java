package org.tencherobert.entities;

import org.tencherobert.base.CelestialBody;

public class Sun extends CelestialBody {

    public Sun(String texturePath, float sphereSize, int x, int y, int z) {
        super(texturePath, sphereSize, x,y,z);
    }
}
